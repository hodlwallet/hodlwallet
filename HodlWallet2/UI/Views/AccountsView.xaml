<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:HodlWallet2.UI.Controls"
    xmlns:vm="clr-namespace:HodlWallet2.Core.ViewModels"
    x:Class="HodlWallet2.UI.Views.AccountsView">
    <ContentPage.BindingContext>
        <vm:AccountsViewModel />
    </ContentPage.BindingContext>
    <FlexLayout Direction="Column"
                JustifyContent="SpaceAround">
        <FlexLayout Direction="Row"
                    FlexLayout.Grow="2"
                    JustifyContent="SpaceAround">
            <Label Text="{Binding AccountsTitle}" />
            <Label Text="{Binding PriceText}" />
        </FlexLayout>
        <controls:AccountCollectionView FlexLayout.Grow="16" />
        <Button Command="{Binding AddAccountCommand}"
                BackgroundColor="{DynamicResource BackgroundSecondary}"
                FlexLayout.AlignSelf="Stretch"
                FlexLayout.Grow="4"
                FontAttributes="Bold"
                FontSize="Large"
                Style="{DynamicResource CustomButton}"
                Text="+"/>
    </FlexLayout>
</ContentPage>
