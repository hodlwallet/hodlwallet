<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="HodlWallet2.Views.BackupRecoveryWordView">
    <ContentPage.Content>
        <FlexLayout Direction="Column" BackgroundColor="{StaticResource GrayBackground}">
            <Label  x:Name="Title"
                    Style="{DynamicResource CustomLabel}"
                    FlexLayout.AlignSelf="Center"
                    FlexLayout.Grow="1"
                    Margin="0, 20, 0, 0" />
            <Label x:Name="Header"
                   FontFamily="{StaticResource ElectrolizeFont}"
                   TextColor="{StaticResource SyncGradientStart}"
                   HorizontalTextAlignment="Center"
                   FlexLayout.AlignSelf="Center"
                   FlexLayout.Grow="1" />
            <FlexLayout FlexLayout.Grow="2" />
            <Button x:Name="Word"
                   Style="{DynamicResource CustomButton}"
                   BackgroundColor="{StaticResource DarkGray}"
                   FontSize="Medium"
                   Margin="40"
                   FlexLayout.Grow="1" />
            <Label x:Name="Position"
                   FontFamily="{StaticResource ElectrolizeFont}"
                   TextColor="Gray"
                   FontSize="Micro" />
            <FlexLayout FlexLayout.Grow="2" />
            <Button x:Name="Next"
                    Style="{DynamicResource CustomButton}"
                    BackgroundColor="{StaticResource DarkGray}"
                    CornerRadius="0"
                    FontSize="Large"
                    FlexLayout.AlignSelf="Stretch"
                    FlexLayout.Grow="1" />
        </FlexLayout>
    </ContentPage.Content>
</ContentPage>
