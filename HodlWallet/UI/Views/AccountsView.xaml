<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:HodlWallet.UI.Controls"
    xmlns:vm="clr-namespace:HodlWallet.Core.ViewModels"
    x:Class="HodlWallet.UI.Views.AccountsView">
    <ContentPage.BindingContext>
        <vm:AccountsViewModel />
    </ContentPage.BindingContext>
    <AbsoluteLayout>
        <FlexLayout Direction="Column"
                    JustifyContent="SpaceAround">
            <FlexLayout Direction="Row"
                        FlexLayout.Grow="2"
                        JustifyContent="SpaceAround">
                <Label Text="{Binding AccountsTitle}" />
                <Label Text="{Binding PriceText}" />
            </FlexLayout>
            <controls:AccountCollectionView FlexLayout.Grow="14" />
            <Button BackgroundColor="{DynamicResource BackgroundSecondary}"
                    Clicked="AddAccountButton_Clicked"
                    FlexLayout.AlignSelf="Stretch"
                    FlexLayout.Grow="4"
                    FontAttributes="Bold"
                    FontSize="Large"
                    Style="{DynamicResource CustomButton}"
                    Text="+"/>
        </FlexLayout>
    </AbsoluteLayout>
</ContentPage>
