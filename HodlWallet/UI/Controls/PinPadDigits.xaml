<?xml version="1.0" encoding="UTF-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HodlWallet.UI.Controls.PinPadDigits"
             BackgroundColor="{DynamicResource Bg}">
    <Grid
        x:Name="InputGrid"
        Margin="0,0,0,0"
        HorizontalOptions="CenterAndExpand">

        <Grid.RowDefinitions>
            <RowDefinition Height="15" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="15" />
        </Grid.ColumnDefinitions>

        <Ellipse x:Name="Pin1"
                 Fill="{DynamicResource Fg5}"
                 Grid.Row="0"
                 Grid.Column="0" />
        <BoxView Grid.Row="0" Grid.Column="1" />

        <Ellipse x:Name="Pin2"
                 Fill="{DynamicResource Fg5}"
                 Grid.Row="0"
                 Grid.Column="2" />
        <BoxView Grid.Row="0" Grid.Column="3" />

        <Ellipse x:Name="Pin3"
                 Fill="{DynamicResource Fg5}"
                 Grid.Row="0"
                 Grid.Column="4" />
        <BoxView Grid.Row="0" Grid.Column="5" />

        <Ellipse x:Name="Pin4"
                 Fill="{DynamicResource Fg5}"
                 Grid.Row="0"
                 Grid.Column="6" />
        <BoxView Grid.Row="0" Grid.Column="7" />

        <Ellipse x:Name="Pin5"
                 Fill="{DynamicResource Fg5}"
                 Grid.Row="0"
                 Grid.Column="8" />
        <BoxView Grid.Row="0" Grid.Column="9" />

        <Ellipse x:Name="Pin6"
                 Fill="{DynamicResource Fg5}"
                 Grid.Row="0"
                 Grid.Column="10" />

    </Grid>

    <BoxView HeightRequest="10" />

    <FlexLayout
        JustifyContent="SpaceEvenly"
        FlexLayout.Grow="1" >

        <Button x:Name="button1"
                Text="1"
                HorizontalOptions="CenterAndExpand"
                Style="{DynamicResource LoginDigitButton}" />

        <Button x:Name="button2"
                Text="2"
                HorizontalOptions="CenterAndExpand"
                Style="{DynamicResource LoginDigitButton}" />

        <Button x:Name="button3"
                Text="3"
                HorizontalOptions="CenterAndExpand"
                Style="{DynamicResource LoginDigitButton}" />

    </FlexLayout>

    <FlexLayout
        JustifyContent="SpaceEvenly"
        FlexLayout.Grow="1">

        <Button x:Name="button4"
                Text="4"
                HorizontalOptions="CenterAndExpand"
                Style="{DynamicResource LoginDigitButton}" />

        <Button x:Name="button5"
                Text="5"
                HorizontalOptions="CenterAndExpand"
                Style="{DynamicResource LoginDigitButton}" />

        <Button x:Name="button6"
                Text="6"
                HorizontalOptions="CenterAndExpand"
                Style="{DynamicResource LoginDigitButton}" />

    </FlexLayout>

    <FlexLayout
        JustifyContent="SpaceEvenly"
        FlexLayout.Grow="1" >

        <Button x:Name="button7"
                Text="7"
                HorizontalOptions="CenterAndExpand"
                Style="{DynamicResource LoginDigitButton}" />

        <Button x:Name="button8"
                Text="8"
                HorizontalOptions="CenterAndExpand"
                Style="{DynamicResource LoginDigitButton}" />

        <Button x:Name="button9"
                Text="9"
                HorizontalOptions="CenterAndExpand"
                Style="{DynamicResource LoginDigitButton}" />

    </FlexLayout>

    <FlexLayout
        JustifyContent="SpaceEvenly"
        FlexLayout.Grow="1">

            <Button Text=""
                    Style="{DynamicResource LoginDigitButton}"
                    HorizontalOptions="CenterAndExpand"
                    IsEnabled="False"/>

            <Button x:Name="button0"
                Text="0"
                Clicked="PinButtonClicked"
                HorizontalOptions="CenterAndExpand"
                Style="{DynamicResource LoginDigitButton}" />

            <Frame Padding="{OnPlatform iOS=0}"
                   BackgroundColor="Transparent" >
                <ImageButton IsEnabled="True"
                             HeightRequest="40"
                             WidthRequest="40"
                             CornerRadius="0"
                             Margin="{OnPlatform Android='4,-8,15,-8',iOS='-5,0,5,0'}"
                             BackgroundColor="Transparent"
                             Clicked="BackspaceClicked"
                             x:Name="buttonImg">
                    <ImageButton.Source>
                        <FontImageSource BindingContext="{x:Reference Name=buttonImg}"
                                         Glyph="backspace"
                                         Color="{DynamicResource FgSuccess}"
                                         FontFamily="MaterialIcons-Regular" />
                    </ImageButton.Source>
                </ImageButton>
            </Frame>

    </FlexLayout>
</StackLayout>
