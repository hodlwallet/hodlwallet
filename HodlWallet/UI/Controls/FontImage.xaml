<?xml version="1.0" encoding="utf-8" ?>
<Image xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:controls="clr-namespace:HodlWallet.UI.Controls"
       x:DataType="controls:FontImage"
       x:Class="HodlWallet.UI.Controls.FontImage"
       HeightRequest="40"
       WidthRequest="40"
       VerticalOptions="Center"
       x:Name="self">
    <Image.Source>
        <FontImageSource BindingContext="{x:Reference self}"
                         FontFamily="{Binding FontFamily}"
                         Color="{Binding Color}"
                         Glyph="{Binding Glyph}" />
    </Image.Source>
</Image>